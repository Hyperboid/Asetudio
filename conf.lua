function love.conf(t)
    t.identity = "asetudio"
    t.version = "11.0"

    -- t.window = false

    -- I can't imagine running this on mobile but hey might as well
    t.externalstorage = true
end
